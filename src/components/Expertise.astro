---
import { Code2, Cloud, Network, LayoutGrid } from '@lucide/astro';

const languages = [
  'Java',
  'Python',
  'TypeScript',
  'JavaScript',
  'Spring Boot',
  'JEE',
  'Restlet',
  'React',
  'JUnit',
  'Mockito',
];

const cloud = [
  'GCP (ACE)',
  'Kubernetes',
  'Rancher',
  'Docker',
  'RabbitMQ',
  'Jenkins',
  'Linux',
];

const architecture = [
  'Microservices',
  'REST APIs',
  'GEOAM / AJAX services',
  'MySQL',
  'MongoDB',
  'Redis',
  'Solr',
  'Git',
];
---

<section id="expertise" class="mb-40 px-6 md:px-12 lg:px-24">
  <div
    class="mb-16 flex items-end justify-between border-b border-surface-container-low pb-8 transition-colors"
  >
    <div>
      <h2 class="font-headline mb-2 text-3xl font-bold text-on-background md:text-4xl">Technical Engine</h2>
      <p class="font-body text-on-surface-variant">Core competencies and technological stack.</p>
    </div>
    <LayoutGrid class="h-10 w-10 shrink-0 text-primary opacity-50 md:h-11 md:w-11" stroke-width={1.25} aria-hidden="true" />
  </div>

  <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
    <div
      class="group relative overflow-hidden rounded-2xl border border-outline-variant/15 bg-surface-container-low p-8 transition-colors"
    >
      <div
        class="absolute right-0 top-0 h-32 w-32 rounded-bl-full bg-primary/5 transition-transform group-hover:scale-110"
        aria-hidden="true"
      >
      </div>
      <div class="mb-8 flex items-center gap-4">
        <div
          class="flex h-12 w-12 items-center justify-center rounded-lg border border-outline-variant/20 bg-surface-container-lowest"
        >
          <Code2 class="h-6 w-6 text-primary" stroke-width={1.5} aria-hidden="true" />
        </div>
        <h3 class="font-headline text-xl font-bold text-on-background">Languages &amp; Frameworks</h3>
      </div>
      <div class="flex flex-wrap gap-3">
        {
          languages.map((tag) => (
            <span class="rounded-md border border-outline-variant/20 bg-surface-container-lowest px-4 py-2 font-body text-sm text-on-surface transition-colors">
              {tag}
            </span>
          ))
        }
      </div>
    </div>

    <div
      class="group relative overflow-hidden rounded-2xl border border-outline-variant/15 bg-surface-container-low p-8 transition-colors"
    >
      <div
        class="absolute right-0 top-0 h-32 w-32 rounded-bl-full bg-primary/5 transition-transform group-hover:scale-110"
        aria-hidden="true"
      >
      </div>
      <div class="mb-8 flex items-center gap-4">
        <div
          class="flex h-12 w-12 items-center justify-center rounded-lg border border-outline-variant/20 bg-surface-container-lowest"
        >
          <Cloud class="h-6 w-6 text-primary" stroke-width={1.5} aria-hidden="true" />
        </div>
        <h3 class="font-headline text-xl font-bold text-on-background">Cloud &amp; DevOps</h3>
      </div>
      <div class="flex flex-wrap gap-3">
        {
          cloud.map((tag) => (
            <span class="rounded-md border border-outline-variant/20 bg-surface-container-lowest px-4 py-2 font-body text-sm text-on-surface transition-colors">
              {tag}
            </span>
          ))
        }
      </div>
    </div>

    <div
      class="group relative overflow-hidden rounded-2xl border border-outline-variant/15 bg-surface-container-low p-8 transition-colors"
    >
      <div
        class="absolute right-0 top-0 h-32 w-32 rounded-bl-full bg-primary/5 transition-transform group-hover:scale-110"
        aria-hidden="true"
      >
      </div>
      <div class="mb-8 flex items-center gap-4">
        <div
          class="flex h-12 w-12 items-center justify-center rounded-lg border border-outline-variant/20 bg-surface-container-lowest"
        >
          <Network class="h-6 w-6 text-primary" stroke-width={1.5} aria-hidden="true" />
        </div>
        <h3 class="font-headline text-xl font-bold text-on-background">Architecture &amp; Data</h3>
      </div>
      <div class="flex flex-wrap gap-3">
        {
          architecture.map((tag) => (
            <span class="rounded-md border border-outline-variant/20 bg-surface-container-lowest px-4 py-2 font-body text-sm text-on-surface transition-colors">
              {tag}
            </span>
          ))
        }
      </div>
    </div>
  </div>
</section>
