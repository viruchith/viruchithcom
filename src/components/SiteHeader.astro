---
import { Menu, X } from '@lucide/astro';

const links = [
  { href: '/#projects', label: 'Projects' },
  { href: '/#experience', label: 'Experience' },
  { href: '/articles/', label: 'Articles' },
  { href: '/#education', label: 'Education' },
  { href: '/#contact', label: 'Contact' },
];
---

<header
  class="fixed top-0 z-50 w-full border-b border-white/5 bg-[#0b1326]/78 shadow-nav-dark backdrop-blur-2xl transition-colors duration-300"
>
  <div class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-6 py-5 md:px-8">
    <a
      href="/"
      class="font-headline text-lg font-extrabold tracking-[-0.08em] text-on-background sm:text-xl"
    >
      VIRUCHITH.COM
    </a>

    <nav class="hidden items-center gap-6 md:flex md:gap-8" aria-label="Primary">
      {
        links.map((l) => (
          <a
            class="font-body text-sm font-medium uppercase tracking-[0.16em] text-on-surface-variant transition-colors hover:text-primary"
            href={l.href}
          >
            {l.label}
          </a>
        ))
      }
      <a
        class="stitch-gradient-primary ml-2 scale-95 rounded-full px-6 py-2.5 text-sm font-semibold uppercase tracking-[0.16em] transition-opacity hover:opacity-90 active:duration-100"
        href="https://www.linkedin.com/in/viruchith/"
        target="_blank"
      >
        Hire Me
      </a>
    </nav>

    <div class="flex items-center gap-2 md:hidden">
      <button
        type="button"
        class="flex h-10 w-10 items-center justify-center rounded-full text-on-background transition hover:bg-white/10"
        aria-expanded="false"
        aria-controls="mobile-nav"
        data-mobile-menu-open
      >
        <span class="menu-icon-open"><Menu class="h-6 w-6" stroke-width={1.5} aria-hidden="true" /></span>
        <span class="menu-icon-close hidden"><X class="h-6 w-6" stroke-width={1.5} aria-hidden="true" /></span>
        <span class="sr-only">Open menu</span>
      </button>
    </div>
  </div>

  <div
    id="mobile-nav"
    class="hidden border-t border-white/10 bg-[#0b1326]/95 px-6 py-6 backdrop-blur-xl md:hidden"
    data-mobile-panel
  >
    <nav class="flex flex-col gap-4" aria-label="Mobile">
      {
        links.map((l) => (
          <a
            class="font-body text-sm font-medium uppercase tracking-[0.16em] text-on-surface transition hover:text-primary"
            href={l.href}
            data-mobile-link
          >
            {l.label}
          </a>
        ))
      }
      <a class="stitch-gradient-primary mt-2 inline-flex justify-center rounded-full px-6 py-3 text-sm font-semibold uppercase tracking-[0.16em]" href="/#contact">
        Hire Me
      </a>
    </nav>
  </div>
</header>

<script>
  function initMobileNav() {
    const btn = document.querySelector<HTMLElement>('[data-mobile-menu-open]');
    const panel = document.querySelector<HTMLElement>('[data-mobile-panel]');
    const openIcon = document.querySelector('.menu-icon-open');
    const closeIcon = document.querySelector('.menu-icon-close');
    if (!btn) return;
    if (!panel) return;
    const menuBtn = btn;
    const menuPanel = panel;
    if (menuBtn.dataset.navInit) return;
    menuBtn.dataset.navInit = '1';

    function setOpen(isOpen: boolean) {
      menuPanel.classList.toggle('hidden', !isOpen);
      menuBtn.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
      openIcon?.classList.toggle('hidden', isOpen);
      closeIcon?.classList.toggle('hidden', !isOpen);
    }

    menuBtn.addEventListener('click', function () {
      setOpen(menuPanel.classList.contains('hidden'));
    });

    menuPanel.querySelectorAll<HTMLElement>('[data-mobile-link]').forEach(function (link) {
      link.addEventListener('click', function () {
        setOpen(false);
      });
    });
  }

  initMobileNav();
  document.addEventListener('astro:after-swap', initMobileNav);
</script>
